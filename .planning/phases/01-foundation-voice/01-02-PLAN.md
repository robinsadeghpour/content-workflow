---
phase: 01-foundation-voice
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/pulse/SKILL.md
  - .claude/skills/review/SKILL.md
autonomous: true
requirements: [INFR-02, INFR-03, INFR-04]
must_haves:
  truths:
    - "/pulse slash command exists and responds when invoked"
    - "/review slash command exists and responds when invoked"
    - "Stub skills document their intended Phase 2 behavior clearly"
    - "Cron schedule entries are documented for 6 AM pulse and 6 PM perf-check"
  artifacts:
    - path: ".claude/skills/pulse/SKILL.md"
      provides: "Stub slash command for daily discovery trigger"
      contains: "name: pulse"
    - path: ".claude/skills/review/SKILL.md"
      provides: "Stub slash command for morning review workflow"
      contains: "name: review"
  key_links:
    - from: ".claude/skills/pulse/SKILL.md"
      to: "data/content.db"
      via: "documented future dependency"
      pattern: "data/content\\.db"
    - from: ".claude/skills/review/SKILL.md"
      to: "data/content.db"
      via: "documented future dependency"
      pattern: "data/content\\.db"
---

<objective>
Create stub CLI skills for /pulse and /review, and document the cron schedule registration for daily automation triggers.

Purpose: INFR-02 requires all interaction via CLI slash commands. INFR-04 requires cron automation skeleton. These stubs establish the command surface that Phase 2 will fill with real logic. INFR-03 (parallel subagent execution) is documented in the writing skill's allowed-tools pattern (Task tool for concurrent platform drafts).
Output: Two stub skills that register as slash commands, with documented cron schedule entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-voice/01-CONTEXT.md
@.planning/phases/01-foundation-voice/01-RESEARCH.md

<interfaces>
Existing skill SKILL.md frontmatter pattern (from humanizer):
  YAML frontmatter with fields: name, version, description (multiline), allowed-tools (list)
  Delimiter: triple-dash on its own line at start and end of frontmatter
  See .claude/skills/humanizer/SKILL.md for exact format

Cron pattern from tiktok-slideshows SKILL.md:
  9:00 AM - Morning post
  2:00 PM - Afternoon post
  5:00 PM - Experimental post
  7:00 PM - Evening post
  10:00 AM - Daily performance check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /pulse stub skill</name>
  <files>.claude/skills/pulse/SKILL.md</files>
  <read_first>
    - /Users/robinsadeghpour/content-workflow/.claude/skills/humanizer/SKILL.md (SKILL.md frontmatter pattern: name, version, description, allowed-tools format)
    - /Users/robinsadeghpour/content-workflow/.claude/skills/tiktok-slideshows/SKILL.md (cron automation patterns, first 40 lines for schedule format)
    - /Users/robinsadeghpour/content-workflow/.planning/phases/01-foundation-voice/01-CONTEXT.md (D-07: Claude Code /schedule for triggers, D-08: stub triggers only, D-09: individual slash commands, D-10: /pulse stub)
  </read_first>
  <action>
    Create the directory `.claude/skills/pulse/` and the file `.claude/skills/pulse/SKILL.md`.

    The SKILL.md must have YAML frontmatter (between triple-dash delimiters) with these exact fields:
    - name: pulse
    - version: 0.1.0
    - description: multiline string explaining this is a daily content discovery trigger that scrapes YouTube, X, TikTok, web, and changelog sources for trending AI/tech topics, writes discovered ideas to data/content.db. Trigger phrases: /pulse, "run discovery", "find topics", or triggered by daily 6 AM schedule.
    - allowed-tools: Read, Bash, Glob, Grep, Write, AskUserQuestion

    After the frontmatter, the body must contain:

    A level-1 heading: "Pulse -- Content Discovery Trigger"

    A prominent blockquote: "Phase 1 stub. Phase 2 will implement full scraping logic (DISC-01, DISC-02)."

    Section "Current Behavior (Phase 1)": Explain this is a stub. When invoked, respond with a log line:
    [PULSE] {current ISO timestamp} -- pulse triggered (stub, no scraping)
    Status: Awaiting Phase 2 implementation
    Intended sources: YouTube, X, TikTok, web, changelogs
    Target database: data/content.db (ideas table)

    Section "Intended Behavior (Phase 2)": List the 4 steps: scrape sources, score/dedup against content.db, write to ideas table, present top 5 summary.

    Section "Schedule":
    - Daily trigger: 6:00 AM via Claude Code /schedule (per D-07)
    - Manual trigger: User runs /pulse anytime
    - Registration: /schedule "0 6 * * *" /pulse

    Section "Dependencies (Phase 2)":
    - data/content.db -- ideas table for writing discovered topics
    - APIFY_TOKEN in .env -- for Apify-based scraping actors
    - Apify skill -- for YouTube, TikTok, X data extraction
    - Supadata skill -- for transcript extraction from discovered videos

    Per D-08: stub triggers only. Per D-09: individual slash command (not namespaced). Per D-10: /pulse scaffolded in Phase 1.
  </action>
  <verify>
    <automated>test -f /Users/robinsadeghpour/content-workflow/.claude/skills/pulse/SKILL.md && head -3 /Users/robinsadeghpour/content-workflow/.claude/skills/pulse/SKILL.md | grep -q "name: pulse" && echo "pulse SKILL.md OK"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/pulse/SKILL.md exists
    - File contains "name: pulse" in frontmatter
    - File contains "version: 0.1.0"
    - File contains "Phase 1 stub" prominently
    - File contains "Phase 2 will implement"
    - File contains "data/content.db"
    - File contains '/schedule "0 6 * * *" /pulse'
    - File contains allowed-tools block with Read, Bash, Glob, Grep, Write, AskUserQuestion
  </acceptance_criteria>
  <done>/pulse slash command is registered via SKILL.md. It clearly documents stub status, intended Phase 2 behavior, cron schedule entry, and all dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create /review stub skill with cron documentation</name>
  <files>.claude/skills/review/SKILL.md</files>
  <read_first>
    - /Users/robinsadeghpour/content-workflow/.claude/skills/pulse/SKILL.md (just-created stub pattern from Task 1 for consistency)
    - /Users/robinsadeghpour/content-workflow/.planning/phases/01-foundation-voice/01-CONTEXT.md (D-07, D-08, D-10 decisions)
  </read_first>
  <action>
    Create the directory `.claude/skills/review/` and the file `.claude/skills/review/SKILL.md`.

    The SKILL.md must have YAML frontmatter (between triple-dash delimiters) with these exact fields:
    - name: review
    - version: 0.1.0
    - description: multiline string explaining this is morning batch review of discovered content ideas, presents top ideas for KEEP/SKIP/STAR decisions via CLI. Trigger phrases: /review, "morning review", "show me ideas", or "what should I post about".
    - allowed-tools: Read, Write, Bash, Glob, Grep, AskUserQuestion

    After the frontmatter, the body must contain:

    A level-1 heading: "Review -- Morning Batch Content Review"

    A prominent blockquote: "Phase 1 stub. Phase 2 will implement the full review workflow (DISC-03)."

    Section "Current Behavior (Phase 1)": Explain this is a stub. When invoked, respond with:
    [REVIEW] {current ISO timestamp} -- review triggered (stub, no ideas to review)
    Status: Awaiting Phase 2 implementation
    Source: data/content.db (ideas table, status='new' or status='starred')
    Workflow: Present top ideas -> KEEP/SKIP/STAR -> update status

    Section "Intended Behavior (Phase 2)": List the 5 steps: query data/content.db for new/starred ideas ordered by score, present each as a card (title, summary, source, score), Robin decides KEEP/SKIP/STAR, update status in database, kept ideas available for Phase 3 content generation.

    Section "Schedule":
    - Manual trigger: User runs /review during morning session
    - No cron: Review is human-initiated (not automated)

    Section "Cron Schedule Summary (all pipeline triggers)": A markdown table with columns Time, Command, Phase, Status:
    - 6:00 AM | /pulse | Phase 2 | Stub
    - Manual | /review | Phase 2 | Stub
    - Manual | /writing | Phase 1 | Active
    - 6:00 PM | perf-check | Phase 4 | Not yet created

    Below the table, registration commands (per D-07):
    - /schedule "0 6 * * *" /pulse
    - /schedule "0 18 * * *" perf-check (Phase 4 creates this skill)

    Section "Dependencies (Phase 2)":
    - data/content.db -- ideas table populated by /pulse
    - Ideas must exist with status='new' -- requires /pulse to have run at least once

    Per D-08: stub triggers only. Per D-10: /review scaffolded in Phase 1.
    This task also serves as the central documentation point for all cron schedule entries (INFR-04).
    INFR-03 (parallel subagent execution) is addressed by Claude Code's native Task tool -- no custom infrastructure needed. The writing skill (Plan 03) will use Skill() invocations, and Phase 3 plans will use the Task tool for concurrent multi-platform generation.
  </action>
  <verify>
    <automated>test -f /Users/robinsadeghpour/content-workflow/.claude/skills/review/SKILL.md && head -3 /Users/robinsadeghpour/content-workflow/.claude/skills/review/SKILL.md | grep -q "name: review" && echo "review SKILL.md OK"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/review/SKILL.md exists
    - File contains "name: review" in frontmatter
    - File contains "version: 0.1.0"
    - File contains "Phase 1 stub" prominently
    - File contains "Phase 2 will implement"
    - File contains "data/content.db"
    - File contains "KEEP/SKIP/STAR"
    - File contains cron schedule summary table with /pulse at 6 AM and perf-check at 6 PM
    - File contains '/schedule "0 6 * * *" /pulse'
    - File contains '/schedule "0 18 * * *" perf-check'
  </acceptance_criteria>
  <done>/review slash command is registered via SKILL.md. Stub status is clear. Cron schedule entries for the entire pipeline are documented. INFR-03 parallel execution is addressed via Claude Code native Task tool (no custom code needed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Stub skills produce static log output only -- no user input processed, no data read/written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | pulse/SKILL.md | accept | Stub only -- no logic to tamper with. Phase 2 plan will address scraping input validation. |
| T-01-05 | Tampering | review/SKILL.md | accept | Stub only -- no database queries yet. Phase 2 plan will address SQL parameterization. |
</threat_model>

<verification>
1. /pulse invocation shows stub message with timestamp
2. /review invocation shows stub message with timestamp
3. Both SKILL.md files have valid YAML frontmatter (name, version, description, allowed-tools)
4. Cron schedule entries documented in review/SKILL.md
</verification>

<success_criteria>
- /pulse and /review are registered as Claude Code slash commands via SKILL.md
- Both stubs clearly document Phase 1 status and Phase 2 intended behavior
- Cron schedule entries (6 AM pulse, 6 PM perf-check) are documented
- No existing skills are modified (per D-03, D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-voice/01-02-SUMMARY.md`
</output>
