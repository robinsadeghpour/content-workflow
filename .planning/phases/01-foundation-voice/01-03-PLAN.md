---
phase: 01-foundation-voice
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .claude/skills/writing/SKILL.md
  - .claude/skills/writing/data/voice-linkedin.json
  - .claude/skills/writing/data/voice-casual.json
  - .claude/skills/writing/data/sample-posts-linkedin.md
  - .claude/skills/writing/data/sample-posts-casual.md
autonomous: false
requirements: [VOIC-01, VOIC-02, VOIC-03]
user_setup:
  - service: anthropic
    why: "Voice extraction from scraped posts uses Claude API"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys -> Create key"
  - service: apify (optional)
    why: "Automated scraping of Robin's LinkedIn/TikTok/Instagram posts"
    env_vars:
      - name: APIFY_TOKEN
        source: "Apify Console -> Settings -> Integrations -> API Token"
must_haves:
  truths:
    - "A voice profile for LinkedIn exists with Robin's actual writing patterns extracted from real posts"
    - "A voice profile for TikTok/Instagram casual exists with Robin's casual tone patterns"
    - "The writing skill can be invoked via /writing and applies the correct voice profile per platform"
    - "The writing skill calls the humanizer as a subskill to strip AI patterns before applying voice"
    - "Nick Saraev and Lenny Rachitsky structural patterns are extracted and documented separately from Robin's voice"
  artifacts:
    - path: ".claude/skills/writing/SKILL.md"
      provides: "Voice profile management and humanizer orchestration skill"
      contains: "name: writing"
      min_lines: 80
    - path: ".claude/skills/writing/data/voice-linkedin.json"
      provides: "LinkedIn formal-authentic voice profile"
      contains: "formal-authentic"
    - path: ".claude/skills/writing/data/voice-casual.json"
      provides: "TikTok + Instagram casual voice profile"
      contains: "casual"
  key_links:
    - from: ".claude/skills/writing/SKILL.md"
      to: ".claude/skills/humanizer/SKILL.md"
      via: "Skill(humanizer *) in allowed-tools"
      pattern: "Skill\\(humanizer"
    - from: ".claude/skills/writing/SKILL.md"
      to: ".claude/skills/writing/data/voice-linkedin.json"
      via: "Read voice profile by platform argument"
      pattern: "voice-linkedin\\.json"
    - from: ".claude/skills/writing/SKILL.md"
      to: ".claude/skills/writing/data/voice-casual.json"
      via: "Read voice profile by platform argument"
      pattern: "voice-casual\\.json"
---

<objective>
Build Robin's voice profile system and the /writing skill that orchestrates humanizer + voice application.

Purpose: VOIC-01/02/03 require platform-differentiated voice profiles from real posts and a skill that applies them to every draft. This is the core authenticity layer -- without it, all generated content sounds like generic AI.
Output: Working /writing skill with two voice profiles (linkedin, casual) populated from Robin's real posts, plus structural patterns extracted from Nick Saraev and Lenny Rachitsky.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-voice/01-CONTEXT.md
@.planning/phases/01-foundation-voice/01-RESEARCH.md
@.planning/phases/01-foundation-voice/01-01-SUMMARY.md

<interfaces>
Humanizer skill pattern (from .claude/skills/humanizer/SKILL.md):
  YAML frontmatter fields: name (humanizer), version (2.5.1), description (multiline), allowed-tools (Read, Write, Edit, Grep, Glob, AskUserQuestion)
  Key features for writing skill integration:
  - Voice Calibration mode: pass writing samples, humanizer analyzes patterns
  - 12+ AI pattern categories detected and removed
  - Final anti-AI pass: "What makes this obviously AI?" then fix

LinkedIn skill cross-skill invocation pattern (from .claude/skills/linkedin/SKILL.md):
  allowed-tools entry: Skill(humanizer *)

Voice profile JSON structure (from research):
  Top-level fields: version, platform, tone, built_from, last_updated, fingerprint (object), hook_patterns (array), banned_patterns (array), structural_inspiration (object), sample_posts (array)
  fingerprint keys: sentence_length, paragraph_length, opener_style, word_choice, punctuation, first_person, cta_style

TikTok casual voice reference (from tiktok-slideshows SKILL.md):
  lowercase text, short punchy sentences, "brooo"/"bruh" energy, write like texting a friend, focus on OTHER person's reaction not yourself
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect Robin's posts and build voice sample files</name>
  <files>.claude/skills/writing/data/sample-posts-linkedin.md, .claude/skills/writing/data/sample-posts-casual.md</files>
  <read_first>
    - /Users/robinsadeghpour/content-workflow/.claude/skills/apify-ultimate-scraper/SKILL.md (Apify scraper capabilities -- check if LinkedIn/TikTok/Instagram post scraping actors are available)
    - /Users/robinsadeghpour/content-workflow/.planning/phases/01-foundation-voice/01-RESEARCH.md (Pitfall 1: LinkedIn scraping blocked, Pitfall 3: not enough samples, Open Question 2: LinkedIn scraping via Apify)
    - /Users/robinsadeghpour/content-workflow/.planning/phases/01-foundation-voice/01-CONTEXT.md (D-05: scrape Robin + Nick Saraev + Lenny Rachitsky)
  </read_first>
  <action>
    This task collects Robin's real writing samples for voice profile extraction.

    **Step 1: Create skill directory structure**
    Run: mkdir -p /Users/robinsadeghpour/content-workflow/.claude/skills/writing/data

    **Step 2: Attempt automated scraping (best effort)**
    Try to use the Apify skill to scrape Robin's posts from LinkedIn, TikTok, and Instagram. Check if APIFY_TOKEN is set in environment or .env file.

    - If APIFY_TOKEN available: Use the apify-ultimate-scraper skill to attempt scraping Robin's public posts from each platform. Store raw results temporarily.
    - If APIFY_TOKEN not available OR scraping returns fewer than 30 total posts: Proceed to manual fallback.

    **Step 3: Manual fallback (guaranteed path)**
    Ask Robin to provide posts using AskUserQuestion. Frame the request:

    "I need your real posts to build your voice profile. Please provide:

    LinkedIn posts (target: 30+):
    Paste your LinkedIn posts -- the more the better. Include a mix of your best-performing posts, recent posts (last 3-6 months), and different formats (text-only, carousel intros, opinion pieces).

    TikTok/Instagram captions (target: 20+):
    Paste your TikTok and Instagram captions. Include slideshow captions, story-style captions, and any casual platform content.

    You can paste them all at once -- I will sort them. Separate posts with a blank line and '===NEXT===' on its own line."

    **Step 4: Count and validate**
    After receiving posts (scraped or manual), count the total per platform.
    - If LinkedIn < 20: Ask Robin for more, or note the gap
    - If casual (TikTok + Instagram) < 15: Ask Robin for more, or note the gap
    - Report counts to Robin: "Collected X LinkedIn posts and Y casual posts"

    **Step 5: Save sample files**
    Create .claude/skills/writing/data/sample-posts-linkedin.md with all LinkedIn posts, one per section:

    ## Post LI-001
    [full post text]

    ## Post LI-002
    [full post text]

    (Continue for all posts)

    Create .claude/skills/writing/data/sample-posts-casual.md with all TikTok/Instagram posts in the same format using TT-001, IG-001 prefixes.

    **Step 6: Scrape structural inspiration (per D-05)**
    Attempt to fetch public posts from Nick Saraev (linkedin.com/in/nick-saraev) and Lenny Rachitsky (linkedin.com/in/lennyrachitsky) via Apify or WebFetch.
    - Extract ONLY structural patterns: hook formulas, post structure, engagement tactics, CTA styles
    - Do NOT blend their voice/personality -- only their structural techniques
    - If scraping fails: Use publicly known patterns about their posting styles from training knowledge as a starting point, and note this limitation
    - Save structural notes inline in sample-posts-linkedin.md under a "## Structural Inspiration" section at the end
  </action>
  <verify>
    <automated>test -f /Users/robinsadeghpour/content-workflow/.claude/skills/writing/data/sample-posts-linkedin.md && test -f /Users/robinsadeghpour/content-workflow/.claude/skills/writing/data/sample-posts-casual.md && wc -l /Users/robinsadeghpour/content-workflow/.claude/skills/writing/data/sample-posts-linkedin.md | awk '{print "LinkedIn sample lines:", $1}' && wc -l /Users/robinsadeghpour/content-workflow/.claude/skills/writing/data/sample-posts-casual.md | awk '{print "Casual sample lines:", $1}'</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/writing/data/sample-posts-linkedin.md exists and contains at least 20 posts (sections starting with "## Post LI-")
    - .claude/skills/writing/data/sample-posts-casual.md exists and contains at least 10 posts (sections starting with "## Post TT-" or "## Post IG-")
    - sample-posts-linkedin.md contains "## Structural Inspiration" section with Nick Saraev and Lenny Rachitsky patterns
    - No API keys or tokens appear in any sample file
    - Posts are Robin's actual writing (not AI-generated placeholders)
  </acceptance_criteria>
  <done>Robin's real posts are collected and saved as sample files. Structural inspiration from Nick Saraev and Lenny Rachitsky is documented. Minimum thresholds met (20+ LinkedIn, 10+ casual).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create voice profiles and /writing skill</name>
  <files>.claude/skills/writing/SKILL.md, .claude/skills/writing/data/voice-linkedin.json, .claude/skills/writing/data/voice-casual.json</files>
  <read_first>
    - /Users/robinsadeghpour/content-workflow/.claude/skills/writing/data/sample-posts-linkedin.md (Robin's LinkedIn posts just collected in Task 1)
    - /Users/robinsadeghpour/content-workflow/.claude/skills/writing/data/sample-posts-casual.md (Robin's casual posts just collected in Task 1)
    - /Users/robinsadeghpour/content-workflow/.claude/skills/humanizer/SKILL.md (full file -- voice calibration protocol, AI pattern categories, Skill() invocation pattern)
    - /Users/robinsadeghpour/content-workflow/.claude/skills/tiktok-slideshows/SKILL.md (casual tone reference -- first 50 lines for tone guidance)
    - /Users/robinsadeghpour/content-workflow/.claude/skills/tiktok-slideshows/data/active-rules.json (weighted rules schema -- reference for hook_patterns structure)
    - /Users/robinsadeghpour/content-workflow/.planning/phases/01-foundation-voice/01-RESEARCH.md (voice profile JSON structure, Pattern 3 and Pattern 5)
    - /Users/robinsadeghpour/content-workflow/.planning/phases/01-foundation-voice/01-CONTEXT.md (D-04: writing skill owns voice, D-05: inspiration sources, D-06: two profiles)
  </read_first>
  <action>
    **Step 1: Analyze Robin's LinkedIn posts and build voice-linkedin.json**

    Read all posts from sample-posts-linkedin.md. For each post, analyze:
    - Sentence length distribution (short/medium/long, average)
    - Paragraph length (1-line, 2-3 lines, longer)
    - Opening patterns (question? statement? counterintuitive claim? story?)
    - Word choice level (casual/formal/technical, specific vocabulary preferences)
    - Punctuation habits (dashes, ellipses, parentheticals, emojis?)
    - First-person usage frequency and style
    - CTA patterns (question to audience? reflection prompt? link?)
    - Recurring phrases or verbal tics

    Also incorporate structural patterns from the "Structural Inspiration" section (Nick Saraev / Lenny Rachitsky hooks, post structures, engagement tactics) -- but mark these as "structural_inspiration" separate from Robin's voice.

    Create .claude/skills/writing/data/voice-linkedin.json with this exact structure:

    {
      "version": "1.0",
      "platform": "linkedin",
      "tone": "formal-authentic",
      "built_from": "X Robin LinkedIn posts + Nick Saraev/Lenny Rachitsky structural extraction",
      "last_updated": "2026-04-08",
      "fingerprint": {
        "sentence_length": "[ACTUAL observed pattern from posts]",
        "paragraph_length": "[ACTUAL observed pattern]",
        "opener_style": "[ACTUAL observed pattern]",
        "word_choice": "[ACTUAL observed pattern]",
        "punctuation": "[ACTUAL observed pattern]",
        "first_person": "[ACTUAL observed pattern]",
        "cta_style": "[ACTUAL observed pattern]"
      },
      "hook_patterns": [
        {
          "pattern": "[extracted from Robin's actual high-performing posts]",
          "weight": 9.0,
          "examples": ["[real example from posts]"],
          "source": "robin"
        }
      ],
      "structural_inspiration": {
        "nick_saraev": {
          "post_structure": "[observed patterns]",
          "hook_formulas": ["[formulas]"],
          "engagement_tactics": ["[tactics]"]
        },
        "lenny_rachitsky": {
          "post_structure": "[observed patterns]",
          "hook_formulas": ["[formulas]"],
          "engagement_tactics": ["[tactics]"]
        }
      },
      "banned_patterns": [
        "In today's rapidly evolving landscape",
        "The key takeaway here is",
        "Let's dive into",
        "This is a testament to",
        "It's worth noting that",
        "In an era where",
        "At the end of the day",
        "Moving forward",
        "Needless to say",
        "It goes without saying"
      ],
      "sample_count": 0
    }

    CRITICAL: Replace ALL "[ACTUAL observed pattern...]" and "[observed pattern]" placeholders with REAL observations from Robin's posts. The sample_count must reflect the real number of posts analyzed. No placeholder text is acceptable in the final file.

    **Step 2: Analyze Robin's casual posts and build voice-casual.json**

    Read all posts from sample-posts-casual.md. Analyze the same dimensions but for casual voice. Also reference the tiktok-slideshows SKILL.md casual tone guidance ("brooo", "bruh" energy, lowercase, short punchy sentences).

    Create .claude/skills/writing/data/voice-casual.json with the same structure but:
    - "platform": "tiktok_instagram"
    - "tone": "casual"
    - Fingerprint reflecting casual patterns (lowercase, short sentences, emoji usage, slang)
    - Note: German TikTok uses this same profile with DE localization on top (per D-06)

    **Step 3: Create the /writing SKILL.md**

    Create .claude/skills/writing/SKILL.md with YAML frontmatter (triple-dash delimiters) containing:
    - name: writing
    - version: 1.0.0
    - description: multiline string explaining voice profile management and humanizer orchestration, applying Robin's authentic voice to any draft. Trigger phrases: /writing, "apply my voice", "humanize this", "write in my style". Three modes: (1) apply voice to a draft, (2) update voice profile from new samples, (3) show current voice profile summary.
    - allowed-tools: Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion, Skill(humanizer *), Task

    The SKILL.md body must contain these sections:

    HEADING: "Writing -- Voice Profile and Humanizer Orchestration"

    SECTION "Overview": This skill owns Robin's voice profiles and orchestrates the humanizer. Clean separation: Humanizer = removes AI patterns (generic), Writing skill = applies Robin's specific voice (platform-aware).

    SECTION "Mode 1: Apply Voice to Draft (default)":
    Required argument: --platform linkedin|tiktok|instagram|tiktok_de
    Workflow steps:
    1. Read the platform argument. Map to profile: linkedin -> voice-linkedin.json, tiktok/instagram/tiktok_de -> voice-casual.json
    2. Read the target voice profile JSON
    3. Receive the draft text (from user or piped from another skill)
    4. Run humanizer first: Invoke the humanizer skill on the draft text, passing Robin's sample posts as the voice calibration reference. For LinkedIn: pass 3-5 sample posts from sample-posts-linkedin.md. For casual: pass 3-5 sample posts from sample-posts-casual.md.
    5. Apply voice fingerprint on top of humanizer output: check opener matches profile's opener_style, verify sentence length fits profile's patterns, replace any banned_patterns that survived humanizer, apply hook_patterns if the draft needs a stronger hook. For tiktok_de: apply German localization after voice pass (use @anthropic-ai/sdk for natural DE adaptation, not literal translation).
    6. Final anti-AI pass: Ask "What makes this obviously AI?" -- fix any remaining tells.
    7. Return the final draft with a brief diff summary (what changed).

    SECTION "Mode 2: Update Voice Profile": Trigger: "update voice profile", "add these posts to my profile". Steps: receive new samples, append to sample-posts file, re-analyze, update voice JSON, report changes.

    SECTION "Mode 3: Show Profile Summary": Trigger: "show my voice", "what's my writing style". Read both JSONs, present human-readable summary of each voice.

    SECTION "Parallel Subagent Support (INFR-03)": When generating content for multiple platforms simultaneously, use the Task tool to fire parallel subagents -- one per platform. Each reads its own voice profile independently. No blocking between platforms.

    SECTION "Voice Profile Files": Table mapping data/voice-linkedin.json to LinkedIn formal-authentic, data/voice-casual.json to TikTok EN/DE/Instagram casual, and the two sample-posts files.

    SECTION "Important Rules":
    - NEVER generate content without reading the voice profile first
    - ALWAYS require --platform argument -- no implicit defaults (prevents casual voice on LinkedIn)
    - ALWAYS run humanizer before applying voice fingerprint
    - NEVER store API keys in voice profile JSON files
    - For German TikTok: apply casual voice profile first, THEN localize to German
    - Structural inspiration informs post STRUCTURE, not voice -- "write like Robin but structured like Nick"

    Per D-04: writing skill owns voice profiles and orchestrates humanizer.
    Per D-05: voice from Robin's posts, structural patterns from Nick Saraev / Lenny Rachitsky.
    Per D-06: two profiles -- LinkedIn formal-authentic, TikTok+Instagram casual.
    INFR-03 addressed via Task tool documentation for parallel subagent execution.
  </action>
  <verify>
    <automated>cd /Users/robinsadeghpour/content-workflow && test -f .claude/skills/writing/SKILL.md && test -f .claude/skills/writing/data/voice-linkedin.json && test -f .claude/skills/writing/data/voice-casual.json && grep -q "name: writing" .claude/skills/writing/SKILL.md && grep -q "Skill(humanizer" .claude/skills/writing/SKILL.md && node -e "const p = JSON.parse(require('fs').readFileSync('.claude/skills/writing/data/voice-linkedin.json','utf8')); console.log('LinkedIn profile:', p.tone, '- fingerprint keys:', Object.keys(p.fingerprint).length)" && node -e "const p = JSON.parse(require('fs').readFileSync('.claude/skills/writing/data/voice-casual.json','utf8')); console.log('Casual profile:', p.tone, '- fingerprint keys:', Object.keys(p.fingerprint).length)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/writing/SKILL.md exists and contains "name: writing" in frontmatter
    - .claude/skills/writing/SKILL.md contains "Skill(humanizer *)" in allowed-tools
    - .claude/skills/writing/SKILL.md contains "Task" in allowed-tools (for INFR-03)
    - .claude/skills/writing/SKILL.md contains "--platform linkedin|tiktok|instagram|tiktok_de"
    - .claude/skills/writing/SKILL.md contains "NEVER generate content without reading the voice profile"
    - .claude/skills/writing/data/voice-linkedin.json is valid JSON with "tone": "formal-authentic"
    - .claude/skills/writing/data/voice-linkedin.json contains a "fingerprint" object with at least 5 keys
    - .claude/skills/writing/data/voice-linkedin.json contains "banned_patterns" array with at least 5 entries
    - .claude/skills/writing/data/voice-linkedin.json contains "structural_inspiration" with nick_saraev and lenny_rachitsky keys
    - .claude/skills/writing/data/voice-casual.json is valid JSON with "tone": "casual"
    - .claude/skills/writing/data/voice-casual.json contains a "fingerprint" object with at least 5 keys
    - Neither voice profile JSON contains any API key or token value
    - voice-linkedin.json "fingerprint" values are NOT placeholder text (no "[observed pattern]" strings)
    - voice-casual.json "fingerprint" values are NOT placeholder text
  </acceptance_criteria>
  <done>Writing skill is fully functional with two populated voice profiles built from Robin's actual posts. Humanizer integration is wired via Skill(). Platform tone split is enforced by required --platform argument. Parallel subagent pattern documented for INFR-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify voice profile and writing skill</name>
  <what-built>
    Robin's voice profile system: two platform-specific voice profiles (LinkedIn formal-authentic, TikTok/Instagram casual) built from real posts, plus the /writing skill that orchestrates humanizer + voice application.
  </what-built>
  <how-to-verify>
    1. Run /writing show my voice -- verify the profile summary matches your actual writing style
    2. Review voice-linkedin.json: Do the fingerprint patterns (sentence_length, opener_style, word_choice, etc.) accurately describe how you write on LinkedIn?
    3. Review voice-casual.json: Does the casual fingerprint match your TikTok/Instagram tone?
    4. Quick test: Provide a short AI-generated draft and run /writing --platform linkedin on it -- does the output sound like you?
    5. Check structural inspiration section: Are the Nick Saraev / Lenny Rachitsky structural patterns useful and separate from your voice?
  </how-to-verify>
  <resume-signal>Type "approved" if voice profiles are accurate, or describe what needs adjustment (e.g., "my LinkedIn opener is usually X not Y", "I use more emojis than shown")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scraped posts -> voice profile | External post content (Robin's own + public posts) enters the profile system |
| Voice profile -> humanizer | Profile data passed to humanizer skill for voice calibration |
| User draft -> writing skill | Untrusted draft text enters the voice application pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | voice-*.json | mitigate | Voice profiles contain Robin's writing patterns and sample posts -- these are not secrets but should not contain API keys. Verify no ANTHROPIC_API_KEY or APIFY_TOKEN in JSON files. |
| T-01-07 | Tampering | scraped post content | accept | Robin's own posts are trusted content. Nick Saraev / Lenny Rachitsky public posts are treated as read-only reference -- structural patterns extracted, no raw content stored in profiles. |
| T-01-08 | Tampering | draft text input | mitigate | Writing skill processes draft text through humanizer (which reads but does not execute). No eval() or dynamic code execution on draft text. |
</threat_model>

<verification>
1. /writing is available as a slash command
2. /writing show my voice displays both profiles with non-placeholder data
3. voice-linkedin.json is valid JSON, parseable by Node.js
4. voice-casual.json is valid JSON, parseable by Node.js
5. Both profiles have fingerprint values derived from Robin's actual posts (not template placeholders)
6. SKILL.md correctly references humanizer via Skill() and has Task in allowed-tools
7. Robin confirms the profiles match their actual writing style (checkpoint)
</verification>

<success_criteria>
- Voice profiles built from Robin's real posts (not AI-generated placeholders)
- LinkedIn profile captures formal-authentic tone
- Casual profile captures "brooo" energy for TikTok/Instagram
- Structural inspiration from Nick Saraev / Lenny Rachitsky documented separately
- /writing skill orchestrates humanizer + voice application
- Platform argument is required (no accidental cross-platform voice application)
- Robin has verified and approved the voice profiles
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-voice/01-03-SUMMARY.md`
</output>
