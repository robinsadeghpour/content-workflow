---
phase: 06.1
plan: 03
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - .claude/agents/critic.md
autonomous: true
requirements:
  - D-14
  - D-15
  - D-16
  - D-17
  - D-13
  - D-11
must_haves:
  truths:
    - "critic.md evaluate mode scores ONLY hook, facts, economy (no voice dimension)"
    - "critic.md reads data/research/{idea_id}.md alongside the draft and flags any claim not supported by the brief (D-16)"
    - "critic.md economy scoring uses the cut-test heuristic per unit (D-17), NOT a hard slide count cap"
    - "critic.md return JSON uses hook_score/facts_score/economy_score keys (not voice_score/slide_score/factual_score)"
    - "critic.md reads .claude/agents/data/critic-rubric.json and uses the platform weight vector for best-iter scoring reference"
    - "critic.md revise mode explicitly states: output is a complete replacement draft, the orchestrator will NOT merge it with the prior draft"
  artifacts:
    - path: ".claude/agents/critic.md"
      provides: "Rewritten evaluate rubric (3 dimensions) + cut-test economy + research-brief fact grounding + replace-not-merge revise contract"
      contains: "hook_score"
  key_links:
    - from: ".claude/agents/critic.md evaluate mode"
      to: "data/research/{idea_id}.md"
      via: "Read tool for fact grounding"
      pattern: "data/research"
    - from: ".claude/agents/critic.md"
      to: ".claude/agents/data/critic-rubric.json"
      via: "Read tool for threshold/weight lookup"
      pattern: "critic-rubric.json"
---

<objective>
Rewrite the critic agent's evaluate mode to score the three dimensions locked by D-14 (hook, facts, economy), drop voice scoring, implement the cut-test economy heuristic per D-17, ground facts via the research brief per D-16, and make the revise-mode full-replacement contract explicit per D-13.

Purpose: The critic is the quality gate. This plan rewrites its rubric to match the locked decisions and makes its revise output contract unambiguous so the orchestrator (Plan 02) can safely assign the response wholesale.
Output: A critic agent that evaluates drafts on the right dimensions and returns JSON with the right keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-CONTEXT.md
@.planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-RESEARCH.md
@.claude/agents/critic.md
@.claude/agents/data/critic-rubric.json

<interfaces>
From .claude/agents/data/critic-rubric.json (Plan 01 output):
```json
{
  "pass_threshold": 8,
  "max_revisions": 2,
  "dimensions": ["hook", "facts", "economy"],
  "weights": { "tiktok_en": {...}, "tiktok_de": {...}, "instagram": {...}, "linkedin": {...} }
}
```

Research brief file at data/research/{idea_id}.md has YAML frontmatter including `research_thin: true|false`.

Expected critic return JSON (evaluate mode) — this is the contract consumed by the orchestrator Plan 02:
```json
{
  "hook_score": 0-10,
  "hook_feedback": "...",
  "facts_score": 0-10,
  "facts_feedback": "...",
  "economy_score": 0-10,
  "economy_feedback": "...",
  "overall_pass": boolean,
  "rewrite_instructions": "..."
}
```

Expected critic return for revise mode: the complete replacement draft JSON, same shape as the input draft. No additional wrapping.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite evaluate mode — 3 dimensions, cut-test, research-brief fact grounding</name>
  <files>.claude/agents/critic.md</files>
  <read_first>
    - .claude/agents/critic.md (full file — understand current modes, return shape, MANDATORY read block)
    - .planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-CONTEXT.md (D-14, D-15, D-16, D-17)
    - .planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-RESEARCH.md lines 544-560 (Pattern 4 evaluate rubric prose) + line 550 (economy cut-test formula)
  </read_first>
  <action>
Edit `.claude/agents/critic.md`. Make these changes:

1. **Replace "## Before Evaluating" section** so the critic reads the rubric config + research brief BEFORE scoring. New content:

```markdown
## Before Evaluating

MANDATORY READS (in order):

1. `.claude/agents/data/critic-rubric.json` — get pass_threshold (8), max_revisions (2), and the weight vector for your platform.
2. The research brief file passed by the orchestrator (path: `data/research/<idea_id>.md`). You MUST have this open when scoring facts. If the orchestrator did not provide a brief path, fail loud via SendMessage — never score facts without the brief.

Voice authenticity is NOT a critic dimension (D-14). The writing + humanizer pass already handles voice before the critic sees the draft. Do not re-score voice.
```

2. **Replace "## Mode: Evaluate" section entirely** with this new rubric:

```markdown
## Mode: Evaluate

Input you'll receive from the orchestrator:
- Platform (tiktok_en, tiktok_de, instagram, linkedin)
- Draft content JSON
- Source material: idea title, summary, transcript, source_url
- Research brief path (data/research/<idea_id>.md)

Score on exactly 3 dimensions (0-10 each). Pass threshold is 8 per dimension (D-11).

### 1. HOOK STRENGTH (0-10)
Does the first slide / first paragraph stop the scroll? Is there a pattern interrupt, a contrarian claim, a specific number, or an open loop?

- Weak hooks (score <= 5): generic statements, setup sentences, questions without stakes, listicle-style "X ways to Y" without a punch
- Strong hooks (score >= 8): specific number + time frame, contrarian claim, named person/company + action, open loop that forces the read-through

### 2. FACT GROUNDING (0-10)
Re-read `data/research/<idea_id>.md`. For EVERY factual claim in the draft (numbers, names, dates, quoted stats, product references), verify it traces to the brief. Uncited claims score 0 on this dimension. If the brief frontmatter has `research_thin: true`, allow claims from the idea's `summary` / `transcript` fields to pass. No inline citation markers are required from the writer (D-16) — you do the cross-check mentally.

- 10: every claim traces to the brief or the idea source material
- 8-9: minor paraphrase or one borderline claim
- 5-7: one clearly unsupported claim
- 0-4: multiple invented facts or hallucinated statistics

### 3. CONTENT ECONOMY (0-10) — cut-test (D-17)
For each slide (TikTok/Instagram/LinkedIn carousel) or each paragraph (LinkedIn text/personal/infographic), ask: "If I removed this unit, would the draft be worse?" Count the cut-viable units (units where the answer is NO).

Formula: `score = max(0, 10 - cuts * 2)`
- 0 cuts → 10
- 1 cut → 8
- 2 cuts → 6
- 3 cuts → 4
- 4+ cuts → 2 or 0

Do NOT apply a hard slide count cap — economy is a heuristic, not a fixed range. A 9-slide tutorial scoring 10 on economy is fine if every slide earns its place.

### Overall pass
`overall_pass = (hook_score >= 8 AND facts_score >= 8 AND economy_score >= 8)`

Return format (JSON):
```json
{
  "hook_score": 8,
  "hook_feedback": "Strong contrarian opener with specific number. Slide 1 stops the scroll.",
  "facts_score": 9,
  "facts_feedback": "All claims traced to brief. One borderline paraphrase on slide 4.",
  "economy_score": 7,
  "economy_feedback": "Slide 3 restates slide 2 — cut. Slide 6 adds no payoff — cut.",
  "overall_pass": false,
  "rewrite_instructions": "Tighten economy: cut slide 3 (duplicates slide 2) and slide 6 (no payoff). Keep hook and fact grounding as-is."
}
```

On failure, `rewrite_instructions` must be specific and actionable per failed dimension — name the slide/paragraph to fix and what to do.
```

3. **Replace "## Mode: Revise" section** with:

```markdown
## Mode: Revise

Input you'll receive from the orchestrator:
- Previous draft content JSON
- Previous critic feedback JSON (hook/facts/economy scores + rewrite_instructions)
- Research brief path (data/research/<idea_id>.md) — re-read it to ground any new factual claims

Your task: Fix ONLY the issues identified in the rewrite_instructions. Preserve everything that passed. Produce a COMPLETE replacement draft in the same JSON structure as the input draft.

CRITICAL (D-13): Your output is the new authoritative draft. The orchestrator will NOT merge your output with the prior draft — it assigns your output wholesale as the new current draft. This means:
- You MUST return the full draft structure, not a partial patch
- Every top-level key from the input draft must be present in your output (even if unchanged)
- Every array (slides, etc.) must be the complete final array, not a diff

Return format: The complete revised content JSON in the same structure as the input draft. No markdown fences, no extra text — just the JSON.
```

4. **Update the "## Communication" section** so the status note examples match the new dimension names. Replace any `voice:8 slide:9 factual:10` examples with `hook:8 facts:9 economy:7`.

5. Remove the old frontmatter description line about "voice authenticity, slide quality, and factual accuracy" and replace with: `description: Evaluates content drafts on hook strength, fact grounding (against research brief), and content economy (cut-test). Scores 0-10 per dimension, returns structured feedback. Handles revision when drafts fail. One fresh invocation per draft.`
  </action>
  <verify>
    <automated>grep -q "hook_score" .claude/agents/critic.md &amp;&amp; grep -q "facts_score" .claude/agents/critic.md &amp;&amp; grep -q "economy_score" .claude/agents/critic.md &amp;&amp; grep -q "cut-test" .claude/agents/critic.md &amp;&amp; grep -q "data/research" .claude/agents/critic.md &amp;&amp; grep -q "critic-rubric.json" .claude/agents/critic.md &amp;&amp; grep -q "D-13" .claude/agents/critic.md &amp;&amp; ! grep -q "voice_score" .claude/agents/critic.md &amp;&amp; ! grep -q "slide_score" .claude/agents/critic.md &amp;&amp; ! grep -q "factual_score" .claude/agents/critic.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "hook_score" .claude/agents/critic.md`
    - `grep -q "facts_score" .claude/agents/critic.md`
    - `grep -q "economy_score" .claude/agents/critic.md`
    - `grep -q "HOOK STRENGTH" .claude/agents/critic.md`
    - `grep -q "FACT GROUNDING" .claude/agents/critic.md`
    - `grep -q "CONTENT ECONOMY" .claude/agents/critic.md`
    - `grep -q "cut-test" .claude/agents/critic.md`
    - `grep -q "data/research" .claude/agents/critic.md`
    - `grep -q "critic-rubric.json" .claude/agents/critic.md`
    - `grep -q "D-13" .claude/agents/critic.md`
    - `grep -q "COMPLETE replacement" .claude/agents/critic.md` OR `grep -q "complete replacement" .claude/agents/critic.md`
    - `grep -q "will NOT merge" .claude/agents/critic.md`
    - `grep -q "research_thin: true" .claude/agents/critic.md`
    - ZERO voice/slide/factual dimension references: `! grep -q "voice_score" .claude/agents/critic.md && ! grep -q "slide_score" .claude/agents/critic.md && ! grep -q "factual_score" .claude/agents/critic.md`
    - `grep -q "VOICE AUTHENTICITY" .claude/agents/critic.md` returns nothing (dimension removed): `! grep -q "VOICE AUTHENTICITY" .claude/agents/critic.md`
    - `grep -qE "hook_score >= 8 AND facts_score >= 8 AND economy_score >= 8|overall_pass" .claude/agents/critic.md`
    - `grep -q "score = max(0, 10 - cuts \* 2)" .claude/agents/critic.md` OR `grep -q "10 - cuts" .claude/agents/critic.md`
    - `grep -q "No hard slide" .claude/agents/critic.md` OR `grep -q "not a fixed range\|no hard slide count cap\|not apply a hard slide" .claude/agents/critic.md`
  </acceptance_criteria>
  <done>Critic evaluates on the 3 locked dimensions, grounds facts via the research brief, uses cut-test for economy, and contracts revise-mode as full replacement.</done>
</task>

</tasks>

<verification>
1. Critic scores ONLY hook/facts/economy (grep confirms no voice/slide/factual dimension references)
2. Research brief path is read in evaluate mode
3. Revise mode explicitly contracts full-replacement semantics with D-13 citation
4. Return JSON uses the new key names that the orchestrator Plan 02 loop expects
</verification>

<success_criteria>
- critic.md evaluate mode matches D-14/D-15/D-16/D-17
- critic.md revise mode matches D-13 (full replacement, not merge)
- Return JSON contract matches what orchestrator Plan 02 loop consumes
- Zero dead dimensions (voice/slide/factual) remaining
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-03-SUMMARY.md`
</output>
