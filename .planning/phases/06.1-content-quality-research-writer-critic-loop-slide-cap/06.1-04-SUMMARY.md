---
phase: 06.1
plan: 04
subsystem: dashboard + skill-docs
tags: [dashboard, ui, badges, skill-doc, research-thin, did-not-pass]
requires:
  - 06.1-01 # persisted research_thin + did_not_pass_critic columns
  - 06.1-02 # orchestrator sets the flags
  - 06.1-03 # critic rubric produces did_not_pass signal
provides:
  - "Dashboard kanban cards render research-thin + did-not-pass advisory badges"
  - "generate-content SKILL.md documents research-first flow (Step 1.5) + flag semantics"
affects:
  - scripts/dashboard/public/app.js
  - scripts/dashboard/public/style.css
  - .claude/skills/generate-content/SKILL.md
tech-stack:
  added: []
  patterns:
    - "Conditional badge rendering keyed off SQLite INTEGER flags (handles 1 and true)"
key-files:
  created: []
  modified:
    - scripts/dashboard/public/app.js
    - scripts/dashboard/public/style.css
    - .claude/skills/generate-content/SKILL.md
decisions:
  - "Badge values are checked as `=== 1 || === true` because SQLite returns INTEGER but downstream code may coerce to boolean — both paths render correctly"
  - "Amber for research-thin (advisory), red for did-not-pass (failure) — matches severity semantics from 06.1-CONTEXT D-05/D-12"
  - "No API edit needed: Plan 01 added columns as real DB columns, `SELECT d.*` already returns them"
metrics:
  duration: "~5m"
  completed: 2026-04-11
---

# Phase 06.1 Plan 04: Dashboard Flag Visibility + Skill Doc Sync Summary

Surfaced the `research_thin` and `did_not_pass_critic` flags as badges on dashboard draft cards and updated the generate-content SKILL.md to document the research-first flow and critic-loop hardening from phase 06.1.

## What Was Built

### Task 1: Dashboard badges
- `scripts/dashboard/public/app.js` — Added two conditional badge spans in `kanbanCardHtml(d)` immediately after the `visual_approach` badge. Checks `d.research_thin === 1 || d.research_thin === true` and same for `did_not_pass_critic`. Both badges have descriptive `title=` tooltips.
- `scripts/dashboard/public/style.css` — Added `.badge--research-thin` (amber: `#fef3c7` / `#92400e` / `#fbbf24`) and `.badge--did-not-pass` (red: `#fee2e2` / `#991b1b` / `#f87171`). Inherits base `.badge` sizing/padding from existing block.

### Task 2: SKILL.md research-first documentation
- Inserted **Step 1.5 Research pass (D-06)** between steps 1b and 2 — describes NotebookLM primary path, cached `ideas.transcript` usage, `data/research/<idea_id>.md` caching, research-thin fallback, and the "never during pulse" D-01 constraint.
- Replaced step 9 with critic-loop hardening description referencing D-10 (2-revision cap), D-11 (8/10 per-dimension bar), D-13 (revise fully replaces draft), D-14 (hook/facts/economy dimensions), and `did_not_pass_critic` flag on max-iters.
- Added new **## Flags Robin Will See** section after Output to Robin, documenting both flags as advisory.
- Updated the "Do NOT run ..." warning to explain scripts were deleted, pipeline runs via `Agent(content-orchestrator)` with `scripts/research/run-notebooklm.sh`.

## Commits

- `dfd874f` — feat(06.1-04): render research-thin + did-not-pass badges on dashboard cards
- `a51c74c` — docs(06.1-04): document research-first flow + flags in generate-content SKILL

## Deviations from Plan

None — plan executed exactly as written.

## Verification

All acceptance-criteria grep checks passed:

**Task 1:**
- `badge--research-thin`, `badge--did-not-pass`, `d.research_thin`, `d.did_not_pass_critic` all present in `app.js`
- Both badge classes defined in `style.css`
- Tooltips via `title=` attribute present on both badges

**Task 2:**
- `Research pass (D-06)`, `data/research`, `research_thin`, `did_not_pass_critic`, `## Flags Robin Will See`, `research-thin`, `did-not-pass`, `run-notebooklm.sh`, `D-13`, `D-10`, `hook / facts / economy`, `NEVER during` all present in SKILL.md

## Known Stubs

None — both changes are fully wired. Flag values are produced by Plan 01 (schema) / Plan 02 (orchestrator) / Plan 03 (critic rubric) and consumed by the dashboard here.

## Self-Check: PASSED

- FOUND: scripts/dashboard/public/app.js (modified)
- FOUND: scripts/dashboard/public/style.css (modified)
- FOUND: .claude/skills/generate-content/SKILL.md (modified)
- FOUND: commit dfd874f
- FOUND: commit a51c74c
