---
phase: 06.1
plan: 04
type: execute
wave: 3
depends_on: ["06.1-01", "06.1-02", "06.1-03"]
files_modified:
  - scripts/dashboard/public/app.js
  - scripts/dashboard/public/style.css
  - .claude/skills/generate-content/SKILL.md
autonomous: true
requirements:
  - D-01
  - D-08
  - D-12
  - D-05
must_haves:
  truths:
    - "Dashboard renders a research-thin badge on draft cards when research_thin === 1"
    - "Dashboard renders a did-not-pass badge on draft cards when did_not_pass_critic === 1"
    - ".claude/skills/generate-content/SKILL.md documents the research-first flow (Step 1.5) and the two new flags"
    - "SKILL.md no longer references deleted scripts/generate-content.js or scripts/apply-critic.js as anything other than historical"
  artifacts:
    - path: "scripts/dashboard/public/app.js"
      provides: "Badge rendering for research_thin + did_not_pass_critic on kanban draft cards"
    - path: "scripts/dashboard/public/style.css"
      provides: "Badge styles for research-thin (amber) + did-not-pass (red)"
    - path: ".claude/skills/generate-content/SKILL.md"
      provides: "Documented research-first flow + flag semantics"
  key_links:
    - from: "scripts/dashboard/public/app.js kanbanCardHtml"
      to: "d.research_thin, d.did_not_pass_critic"
      via: "conditional badge rendering"
      pattern: "research_thin|did_not_pass_critic"
---

<objective>
Surface the two new flags in the dashboard so Robin can see at a glance which drafts were generated from sparse research (research-thin) or failed the critic loop at max-iters (did-not-pass). Also update the generate-content skill doc to describe the research-first flow + new flags.

Purpose: Closes the feedback loop — flags are persisted (Plan 01), set by the orchestrator (Plan 02), produced by the critic rubric (Plan 03), and now visible to Robin (this plan).
Output: Dashboard draft cards show badges; SKILL.md reflects reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-CONTEXT.md
@.planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-RESEARCH.md
@scripts/dashboard/public/app.js
@scripts/dashboard/public/style.css
@scripts/dashboard/api.js
@.claude/skills/generate-content/SKILL.md

<interfaces>
From scripts/dashboard/api.js /api/drafts endpoint (already returns `d.*`):
Since Plan 01 adds research_thin + did_not_pass_critic as real columns, the API response now includes `d.research_thin` and `d.did_not_pass_critic` automatically. No API edit needed.

From scripts/dashboard/public/app.js line 814-831 (current kanbanCardHtml):
```js
function kanbanCardHtml(d) {
  const p = platformClass(d.platform);
  return `
    <div class="kanban__card" data-id="${escHtml(d.id)}">
      <div class="kanban__card-meta">
        <span class="badge ${p}">${escHtml(d.platform || '')}</span>
        ${d.visual_approach ? `<span class="badge">${escHtml(d.visual_approach)}</span>` : ''}
        <span style="margin-left:auto">${escHtml(relativeTime(d.updated_at))}</span>
      </div>
      ...
```
Add the two new badges immediately after the visual_approach badge.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render research-thin + did-not-pass badges on dashboard kanban cards</name>
  <files>scripts/dashboard/public/app.js, scripts/dashboard/public/style.css</files>
  <read_first>
    - scripts/dashboard/public/app.js lines 810-835 (kanbanCardHtml function)
    - scripts/dashboard/public/style.css (find existing .badge base class and color variants — search for ".badge--tiktok" or ".badge--youtube" to see the pattern)
    - .planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-RESEARCH.md lines 346-376 (Q10 dashboard visibility)
  </read_first>
  <action>
Edit `scripts/dashboard/public/app.js`. In `kanbanCardHtml(d)` (around line 814), add two conditional badge spans immediately after the `visual_approach` badge line. The new lines:

```js
${d.research_thin === 1 || d.research_thin === true ? `<span class="badge badge--research-thin" title="Research was sparse or failed — writer leaned on idea summary/transcript">research-thin</span>` : ''}
${d.did_not_pass_critic === 1 || d.did_not_pass_critic === true ? `<span class="badge badge--did-not-pass" title="Critic loop hit the 2-revision cap without passing — best iteration kept">did-not-pass</span>` : ''}
```

Handle both number (1) and boolean (true) values because SQLite INTEGER columns return numbers but the app may coerce elsewhere.

Edit `scripts/dashboard/public/style.css`. Add two new badge style classes. Find the existing `.badge--tiktok` / `.badge--youtube` style block as a pattern reference, then append:

```css
.badge--research-thin {
  background-color: #fef3c7;
  color: #92400e;
  border: 1px solid #fbbf24;
}

.badge--did-not-pass {
  background-color: #fee2e2;
  color: #991b1b;
  border: 1px solid #f87171;
}
```

(Amber for advisory/research-thin, red for failure/did-not-pass. Match the existing badge sizing/padding from the base `.badge` class — no need to duplicate those.)

Do NOT edit `scripts/dashboard/api.js` — since Plan 01 added the columns as real DB columns, the `SELECT d.*` query already returns them automatically.
  </action>
  <verify>
    <automated>grep -q "badge--research-thin" scripts/dashboard/public/app.js &amp;&amp; grep -q "badge--did-not-pass" scripts/dashboard/public/app.js &amp;&amp; grep -q "d.research_thin" scripts/dashboard/public/app.js &amp;&amp; grep -q "d.did_not_pass_critic" scripts/dashboard/public/app.js &amp;&amp; grep -q "badge--research-thin" scripts/dashboard/public/style.css &amp;&amp; grep -q "badge--did-not-pass" scripts/dashboard/public/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "badge--research-thin" scripts/dashboard/public/app.js`
    - `grep -q "badge--did-not-pass" scripts/dashboard/public/app.js`
    - `grep -q "d.research_thin" scripts/dashboard/public/app.js`
    - `grep -q "d.did_not_pass_critic" scripts/dashboard/public/app.js`
    - Badge render uses truthy check that handles both 1 and true: `grep -qE "research_thin === 1.*research_thin === true|research_thin === true.*research_thin === 1" scripts/dashboard/public/app.js`
    - Both badges have tooltips via `title=` attribute
    - `grep -q "\.badge--research-thin" scripts/dashboard/public/style.css`
    - `grep -q "\.badge--did-not-pass" scripts/dashboard/public/style.css`
    - Badges are inserted inside the `kanbanCardHtml` function: verify by finding `badge--research-thin` between `function kanbanCardHtml` and the next `function ` keyword in app.js (manual review)
  </acceptance_criteria>
  <done>Dashboard draft cards show research-thin and did-not-pass badges when flags are set.</done>
</task>

<task type="auto">
  <name>Task 2: Update generate-content SKILL.md to document research-first flow + flags</name>
  <files>.claude/skills/generate-content/SKILL.md</files>
  <read_first>
    - .claude/skills/generate-content/SKILL.md (full file — understand current "What It Does Step by Step" + "Implementation" sections)
    - .planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-CONTEXT.md (D-06, D-08, D-12)
  </read_first>
  <action>
Edit `.claude/skills/generate-content/SKILL.md`. Make these changes to the "## What It Does (Step by Step)" section:

1. Insert a new step "1.5" between the current step 1 and step 2:

```markdown
1.5. **Research pass (D-06)** — produces a structured research brief at `data/research/<idea_id>.md` via NotebookLM (primary synthesizer per D-02). Uses cached `ideas.transcript` when available, adds the source URL, and supplements with fresh web research (`notebooklm source add-research`). One research pass feeds all 4 platform writers (consistent facts). Falls back to a research-thin stub if NotebookLM is unavailable — the `research_thin` flag is set on every draft so Robin can see which posts were generated from sparse research. Briefs are cached — if `data/research/<idea_id>.md` already exists, it's reused on re-runs. One research pass fires only when Robin invokes `/generate-content` for a kept idea — NEVER during the pulse discovery run (D-01). Only ideas Robin picks get researched.
```

2. Replace step 9 ("Run critic agent as a SEPARATE process...") with:

```markdown
9. **Run critic loop** per platform — scores hook / facts / economy (D-14), per-dimension pass bar at 8/10 (D-11), hard cap of 2 revisions (D-10). Revise output fully replaces the prior draft (D-13 — fixes the old CR-01 merge bug). On max-iters, the best-scored iteration is kept and flagged `did_not_pass_critic = 1`.
```

3. Add a new "## Flags Robin Will See" section after the "## Output to Robin" section:

```markdown
## Flags Robin Will See

Two advisory flags may appear on draft cards in the dashboard and `/approve` view:

- **research-thin** (amber badge): The research pass returned insufficient info (NotebookLM unavailable, auth expired, source timeout, or empty answer). The writer leaned on the idea's own title/summary/transcript. Advisory only — Robin decides whether to regenerate or approve anyway.
- **did-not-pass** (red badge): The critic loop hit its 2-revision cap without scoring >= 8 on every dimension. The best-scored iteration is what you see. Review carefully before approving.

Neither flag blocks approval — they are informational.
```

4. Under "## Implementation", update the "Do NOT run..." warning to:

```markdown
**Do NOT run `node scripts/generate-content.js` or `node scripts/apply-critic.js`** — these scripts were deleted in Phase 06.1. The pipeline now runs entirely through `.claude/agents/content-orchestrator.md` (invoked via `Agent(content-orchestrator)`), which spawns writer and critic sub-agents and invokes `scripts/research/run-notebooklm.sh` for the research pass.
```
  </action>
  <verify>
    <automated>grep -q "Research pass (D-06)" .claude/skills/generate-content/SKILL.md &amp;&amp; grep -q "research_thin" .claude/skills/generate-content/SKILL.md &amp;&amp; grep -q "did_not_pass_critic" .claude/skills/generate-content/SKILL.md &amp;&amp; grep -q "Flags Robin Will See" .claude/skills/generate-content/SKILL.md &amp;&amp; grep -q "run-notebooklm.sh" .claude/skills/generate-content/SKILL.md &amp;&amp; grep -q "D-13" .claude/skills/generate-content/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Research pass (D-06)" .claude/skills/generate-content/SKILL.md`
    - `grep -q "data/research" .claude/skills/generate-content/SKILL.md`
    - `grep -q "research_thin" .claude/skills/generate-content/SKILL.md`
    - `grep -q "did_not_pass_critic" .claude/skills/generate-content/SKILL.md`
    - `grep -q "## Flags Robin Will See" .claude/skills/generate-content/SKILL.md`
    - `grep -q "research-thin" .claude/skills/generate-content/SKILL.md`
    - `grep -q "did-not-pass" .claude/skills/generate-content/SKILL.md`
    - `grep -q "run-notebooklm.sh" .claude/skills/generate-content/SKILL.md`
    - `grep -q "D-10\|2 revisions" .claude/skills/generate-content/SKILL.md`
    - `grep -q "D-13" .claude/skills/generate-content/SKILL.md`
    - `grep -q "hook / facts / economy\|hook, facts, economy" .claude/skills/generate-content/SKILL.md`
    - `grep -qE "NEVER during.*pulse|never.*pulse|D-01" .claude/skills/generate-content/SKILL.md`
  </acceptance_criteria>
  <done>Skill doc reflects the research-first flow, the new flags, and the post-06.1 architecture.</done>
</task>

</tasks>

<verification>
1. Dashboard cards show both badges when flags are set (manual visual check optional)
2. Badge CSS exists and matches naming
3. SKILL.md describes Step 1.5 + critic loop + flags
</verification>

<success_criteria>
- Dashboard surfaces both flags as distinct badges on draft cards
- SKILL.md documents the research-first flow + both flags
- Robin can run `/generate-content <idea_id>` and see the brief produced, the critic loop enforced, and the flags in the dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-content-quality-research-writer-critic-loop-slide-cap/06.1-04-SUMMARY.md`
</output>
