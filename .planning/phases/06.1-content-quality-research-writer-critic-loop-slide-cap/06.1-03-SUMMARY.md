---
phase: 06.1
plan: 03
subsystem: critic-agent-rubric
tags: [critic, rubric, hook, facts, economy, cut-test, research-grounding]
dependency_graph:
  requires:
    - .claude/agents/data/critic-rubric.json (Plan 01)
    - data/research/{idea_id}.md convention (Plan 01)
  provides:
    - Critic agent rewritten to 3-dimension rubric (hook/facts/economy)
    - Revise-mode full-replacement contract (D-13) for orchestrator consumption
  affects:
    - Plan 02 (orchestrator) — now consumes hook_score/facts_score/economy_score keys and assigns revise output wholesale
tech-stack:
  added: []
  patterns:
    - Agent MANDATORY READS block pointing at rubric JSON + research brief
    - Per-dimension pass threshold (D-11) with explicit overall_pass boolean formula
    - Cut-test economy formula `score = max(0, 10 - cuts * 2)` (D-17)
key-files:
  created:
    - .claude/agents/critic.md
  modified: []
decisions:
  - Voice authenticity removed from critic dimensions (D-14) — writing+humanizer owns voice
  - Per-dimension >=8 pass bar, not a weighted score (D-11); rubric weights reserved for best-iter tiebreakers
  - Revise mode output is authoritative; orchestrator assigns wholesale, no merge (D-13)
  - Economy is a heuristic (cut-test), not a hard slide count cap (D-17)
metrics:
  duration: ~5m
  tasks: 1
  commits: 1
  completed: 2026-04-11
requirements: [D-11, D-13, D-14, D-15, D-16, D-17]
---

# Phase 06.1 Plan 03: Critic Rubric Rewrite Summary

Critic agent rewritten to score hook/facts/economy with research-brief fact grounding and explicit full-replacement revise contract.

## What Was Built

### Task 1 — Critic evaluate + revise rewrite
- `.claude/agents/critic.md` (created in tracked tree): frontmatter `description` updated to reference hook/fact/economy dimensions.
- `## Before Evaluating` now mandates reading `.claude/agents/data/critic-rubric.json` and `data/research/<idea_id>.md` in order; voice is explicitly removed as a critic concern (D-14).
- `## Mode: Evaluate` rewritten with three scored dimensions (0-10 each), pass threshold 8 per dimension (D-11):
  - HOOK STRENGTH with weak/strong score anchors
  - FACT GROUNDING with research-brief cross-check, `research_thin: true` fallback (D-16)
  - CONTENT ECONOMY cut-test with `score = max(0, 10 - cuts * 2)` formula (D-17)
- Explicit `overall_pass = (hook_score >= 8 AND facts_score >= 8 AND economy_score >= 8)` formula.
- Return JSON contract uses `hook_score` / `facts_score` / `economy_score` keys plus per-dimension feedback fields and `rewrite_instructions`.
- `## Mode: Revise` rewritten with D-13 citation and "orchestrator will NOT merge" language; enumerates the full-replacement requirements (all top-level keys present, arrays complete).
- Communication status examples updated from `voice:8 slide:9 factual:10` to `hook:8 facts:9 economy:8`.
- **Commit:** `755b30b`

## Verification Results

| Check | Result |
|-------|--------|
| `grep -q hook_score` | OK |
| `grep -q facts_score` | OK |
| `grep -q economy_score` | OK |
| `grep -q "HOOK STRENGTH"` | OK |
| `grep -q "FACT GROUNDING"` | OK |
| `grep -q "CONTENT ECONOMY"` | OK |
| `grep -q cut-test` | OK |
| `grep -q data/research` | OK |
| `grep -q critic-rubric.json` | OK |
| `grep -q D-13` | OK |
| `grep -q "COMPLETE replacement"` | OK |
| `grep -q "will NOT merge"` | OK |
| `grep -q "research_thin: true"` | OK |
| `grep -q "10 - cuts"` | OK |
| `grep -q "No hard slide"` | OK |
| `! grep -q voice_score` | OK (absent) |
| `! grep -q slide_score` | OK (absent) |
| `! grep -q factual_score` | OK (absent) |
| `! grep -q "VOICE AUTHENTICITY"` | OK (absent) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `.claude/agents/critic.md` was untracked, not tracked-and-modified**
- **Found during:** Task 1 initial read
- **Issue:** The plan frontmatter lists `files_modified: .claude/agents/critic.md`, implying an edit-in-place. The file existed as an untracked working-tree file in the main repo and did not exist at all in this sparse worktree. `git ls-files .claude/agents/` showed only `critic-rubric.json` was tracked.
- **Fix:** Read the current untracked content from the main repo (`/Users/robinsadeghpour/content-workflow/.claude/agents/critic.md`), applied the full Plan 03 rewrite, and wrote the rewritten file into this worktree at `.claude/agents/critic.md`. Committed as a new tracked file. Net effect matches the plan's intent: the rubric is now hook/facts/economy, and the file is under version control for the first time.
- **Files modified:** `.claude/agents/critic.md` (created)
- **Commit:** `755b30b`

### Architectural changes
None.

### Auth gates
None.

## Deferred Issues
None — the single task's acceptance criteria all pass.

## Known Stubs
None. The critic.md prompt is complete and directly consumable by the Plan 02 orchestrator loop (which will land in parallel via Plan 02's wave).

## Self-Check: PASSED

- Files:
  - `.claude/agents/critic.md` — FOUND
- Commits:
  - `755b30b` — FOUND in `git log`
- Grep acceptance criteria (presence + absence) — all PASS per verification table above.
