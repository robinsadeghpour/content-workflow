---
phase: 06-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/review/SKILL.md
  - config/schedule-defaults.json
  - .claude/skills/writing/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "/review SKILL.md schedule table reflects perf-check as Active (no 'Not yet created' string anywhere in the file)"
    - "/review SKILL.md Step 5 next-step pointer tells Robin to run /generate-content <idea_id>, not /linkedin or /tiktok-slideshows"
    - "config/schedule-defaults.json contains a perf_check section with multiplier_max and multiplier_min numeric bounds that apply-performance-weights.js can consume via loadMultiplierBounds()"
    - "Writing skill documents that voice profiles use XML format (voice-linkedin.xml, voice-casual.xml), so no future code attempts JSON.parse() on them"
  artifacts:
    - path: ".claude/skills/review/SKILL.md"
      provides: "Review skill with corrected schedule table and Step 5 pointer"
      contains: "/generate-content"
    - path: "config/schedule-defaults.json"
      provides: "Schedule config including perf_check multiplier bounds"
      contains: "perf_check"
    - path: ".claude/skills/writing/SKILL.md"
      provides: "Writing skill with explicit XML voice profile format documentation"
      contains: "XML format"
  key_links:
    - from: "scripts/apply-performance-weights.js"
      to: "config/schedule-defaults.json"
      via: "loadMultiplierBounds() reads config.perf_check.multiplier_max / multiplier_min"
      pattern: "perf_check"
---

<objective>
Close the three tech-debt items flagged in `.planning/v1.0-MILESTONE-AUDIT.md` (lines 60, 64-65, 71, 189-203). All three are mechanical edits with known before/after strings and no design decisions.

Purpose: Eliminate stale references and hardcoded-only defaults so future code/operator tuning has a single source of truth. Document the voice profile XML format deviation so no future JSON.parse() attempt silently breaks.
Output: Corrected `.claude/skills/review/SKILL.md`, extended `config/schedule-defaults.json`, and an explicit format note in `.claude/skills/writing/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/review/SKILL.md
@config/schedule-defaults.json
@scripts/apply-performance-weights.js
@.claude/skills/writing/SKILL.md

<interfaces>
<!-- Key facts extracted from the source files so the executor does not need to hunt. -->

From scripts/apply-performance-weights.js (lines 34-52):
```javascript
const DEFAULT_MULTIPLIER_MAX = 1.5;
const DEFAULT_MULTIPLIER_MIN = 0.7;

function loadMultiplierBounds() {
  try {
    const configPath = path.join(__dirname, '..', 'config', 'schedule-defaults.json');
    const config = JSON.parse(fs.readFileSync(configPath, 'utf-8'));
    const pc = config.perf_check || {};
    return {
      max: typeof pc.multiplier_max === 'number' ? pc.multiplier_max : DEFAULT_MULTIPLIER_MAX,
      min: typeof pc.multiplier_min === 'number' ? pc.multiplier_min : DEFAULT_MULTIPLIER_MIN,
    };
  } catch (e) {
    return { max: DEFAULT_MULTIPLIER_MAX, min: DEFAULT_MULTIPLIER_MIN };
  }
}
```
The script already READS `config.perf_check.multiplier_max` / `multiplier_min` — the config file just never defined them. This task adds the section with values matching the current hardcoded defaults (1.5 / 0.7) so behavior does not change.

From .claude/skills/review/SKILL.md (current state of Schedule table, line ~201):
```
| 6:00 PM | perf-check | Phase 4 | Not yet created |
```
Phase 4 is complete; `scripts/perf-check.js` exists and is scheduled. Status must become "Active".

From .claude/skills/review/SKILL.md (current state of Step 5 footer, line ~176):
```
> "Run /generate-content on any kept idea to produce TikTok EN, TikTok DE, Instagram, and LinkedIn drafts."
```
Actually already references /generate-content in the "If kept > 0" footer — BUT audit line 65 flags that Step 5 references `/linkedin, /tiktok-slideshows`. Double-check during execution: grep for any `/linkedin` or `/tiktok-slideshows` occurrence and, if present, replace with the /generate-content guidance. If it was already cleaned up by a prior commit, mark acceptance as satisfied (no stale references remain).

From .claude/skills/writing/SKILL.md (lines 34-38):
```
Voice profiles live in `.claude/skills/writing/data/`:
- `voice-linkedin.xml` — LinkedIn formal-authentic profile (50 posts analyzed)
- `voice-casual.xml` — TikTok EN/DE + Instagram casual profile (50 posts analyzed)
```
The file names are `.xml` but nowhere is it stated that this is a deliberate deviation from the originally planned JSON format. Add an explicit note so future refactors do not assume JSON.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix /review SKILL.md stale references (schedule table + Step 5 pointer)</name>
  <files>.claude/skills/review/SKILL.md</files>
  <read_first>
    - .claude/skills/review/SKILL.md (full file — specifically the Schedule table around line 196-201 and Step 5 footer around line 175-177)
    - .planning/v1.0-MILESTONE-AUDIT.md lines 64-65 (source of truth for what's stale)
  </read_first>
  <action>
Make two precise edits to `.claude/skills/review/SKILL.md`:

**Edit 1 — Schedule table (around line 201):**
Replace the exact line:
```
| 6:00 PM | perf-check | Phase 4 | Not yet created |
```
with:
```
| 6:00 PM | perf-check | Phase 4 | Active |
```

**Edit 2 — Step 5 stale pointer:**
Grep the file for `/linkedin` and `/tiktok-slideshows`. If either appears anywhere in Step 5 (or anywhere else in the file), replace that sentence/pointer with:
```
> "Run /generate-content <idea_id> on any kept idea to produce TikTok EN, TikTok DE, Instagram, and LinkedIn drafts."
```
The existing Step 5 footer currently reads:
```
> "Run /generate-content on any kept idea to produce TikTok EN, TikTok DE, Instagram, and LinkedIn drafts."
```
Update it to include the `<idea_id>` argument exactly as shown above so the pointer is actionable (matches how Robin actually invokes it).

Do NOT touch any other text in the file.
  </action>
  <verify>
    <automated>grep -c "Not yet created" .claude/skills/review/SKILL.md | grep -q "^0$" && grep -q "| 6:00 PM | perf-check | Phase 4 | Active |" .claude/skills/review/SKILL.md && grep -q "/generate-content <idea_id>" .claude/skills/review/SKILL.md && ! grep -qE "/linkedin|/tiktok-slideshows" .claude/skills/review/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Not yet created" .claude/skills/review/SKILL.md` returns zero matches
    - `grep "| 6:00 PM | perf-check | Phase 4 | Active |" .claude/skills/review/SKILL.md` returns exactly 1 match
    - `grep "/generate-content <idea_id>" .claude/skills/review/SKILL.md` returns at least 1 match
    - `grep -E "/linkedin|/tiktok-slideshows" .claude/skills/review/SKILL.md` returns zero matches
  </acceptance_criteria>
  <done>Schedule table shows perf-check Active; Step 5 pointer references `/generate-content <idea_id>`; no stale `/linkedin` or `/tiktok-slideshows` references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add perf_check section to config/schedule-defaults.json</name>
  <files>config/schedule-defaults.json</files>
  <read_first>
    - config/schedule-defaults.json (current content — 33 lines, JSON with platforms + retry)
    - scripts/apply-performance-weights.js lines 34-53 (source of truth for the default values 1.5 and 0.7, and the key names `multiplier_max` / `multiplier_min`)
    - .planning/v1.0-MILESTONE-AUDIT.md line 71 and 203
  </read_first>
  <action>
Add a new top-level `perf_check` key to `config/schedule-defaults.json` alongside the existing `platforms` and `retry` keys. Use the EXACT values currently hardcoded in `scripts/apply-performance-weights.js` (DEFAULT_MULTIPLIER_MAX=1.5, DEFAULT_MULTIPLIER_MIN=0.7) so runtime behavior does not change.

The final JSON must look like:
```json
{
  "platforms": {
    "linkedin": {
      "integration_id": "FILL_AT_SETUP",
      "default_hour": 9,
      "default_minute": 0,
      "timezone": "Europe/Berlin"
    },
    "tiktok_en": {
      "integration_id": "FILL_AT_SETUP",
      "default_hour": 12,
      "default_minute": 0,
      "timezone": "Europe/Berlin"
    },
    "tiktok_de": {
      "integration_id": "FILL_AT_SETUP",
      "default_hour": 12,
      "default_minute": 30,
      "timezone": "Europe/Berlin"
    },
    "instagram": {
      "integration_id": "FILL_AT_SETUP",
      "default_hour": 18,
      "default_minute": 0,
      "timezone": "Europe/Berlin"
    }
  },
  "retry": {
    "max_attempts": 3,
    "backoff_base_seconds": 5
  },
  "perf_check": {
    "multiplier_max": 1.5,
    "multiplier_min": 0.7
  }
}
```

Preserve 2-space indentation and trailing newline. Validate with `node -e "JSON.parse(require('fs').readFileSync('config/schedule-defaults.json','utf8'))"` after writing.
  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('config/schedule-defaults.json','utf8')); if(c.perf_check && c.perf_check.multiplier_max===1.5 && c.perf_check.multiplier_min===0.7 && c.platforms && c.retry) console.log('PASS'); else { console.error('FAIL', JSON.stringify(c.perf_check)); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `config/schedule-defaults.json` parses as valid JSON
    - Top-level `perf_check` key exists
    - `perf_check.multiplier_max === 1.5`
    - `perf_check.multiplier_min === 0.7`
    - Existing `platforms` and `retry` sections are unchanged (no keys added/removed/modified)
    - `grep '"perf_check"' config/schedule-defaults.json` returns 1 match
    - Running `node scripts/apply-performance-weights.js` in a dry-run context still works (file is only read during `loadMultiplierBounds()`, so syntactic validity is the main check)
  </acceptance_criteria>
  <done>Config file contains perf_check.multiplier_max=1.5 and multiplier_min=0.7; apply-performance-weights.js now reads these values instead of falling through to DEFAULT_* constants.</done>
</task>

<task type="auto">
  <name>Task 3: Document voice profile XML format in writing SKILL.md</name>
  <files>.claude/skills/writing/SKILL.md</files>
  <read_first>
    - .claude/skills/writing/SKILL.md (specifically lines 34-38 where voice profile files are listed, and the "Voice Profile Files" table around lines 153-160)
    - .planning/v1.0-MILESTONE-AUDIT.md line 60 and 192 (the tech debt item)
  </read_first>
  <action>
Add an explicit format-deviation note to `.claude/skills/writing/SKILL.md` so no future code or refactor assumes voice profiles are JSON.

**Edit:** Immediately after the existing line (around line 38):
```
- `sample-posts-casual.md` — Robin's actual X/casual posts (reference corpus)
```
Insert a blank line, then this note block:
```
> **Format note:** Voice profiles use **XML format** (`.xml`), not JSON. This is a deliberate deviation from the originally planned JSON format — XML better represents nested fingerprint/hook-patterns/banned-patterns structure with mixed content. Do NOT call `JSON.parse()` on voice profile files. Read them as text and parse with an XML parser (or regex for simple field extraction) if programmatic access is needed.
```

Leave all other text in the file unchanged.
  </action>
  <verify>
    <automated>grep -q "Format note:" .claude/skills/writing/SKILL.md && grep -q "XML format" .claude/skills/writing/SKILL.md && grep -q "Do NOT call \`JSON.parse()\`" .claude/skills/writing/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Format note:" .claude/skills/writing/SKILL.md` returns at least 1 match
    - `grep "XML format" .claude/skills/writing/SKILL.md` returns at least 1 match
    - `grep "JSON.parse" .claude/skills/writing/SKILL.md` returns at least 1 match (the warning against it)
    - The rest of the file is byte-identical except for the inserted block (diff shows only additions, no deletions)
  </acceptance_criteria>
  <done>Writing skill explicitly documents that voice profiles are XML, not JSON, with a warning against JSON.parse().</done>
</task>

</tasks>

<verification>
After all three tasks complete, run these checks in sequence:

1. `grep -c "Not yet created" .claude/skills/review/SKILL.md` → 0
2. `grep "| 6:00 PM | perf-check | Phase 4 | Active |" .claude/skills/review/SKILL.md` → 1 match
3. `grep "/generate-content <idea_id>" .claude/skills/review/SKILL.md` → ≥1 match
4. `grep -E "/linkedin|/tiktok-slideshows" .claude/skills/review/SKILL.md` → 0 matches
5. `node -e "const c=require('./config/schedule-defaults.json'); console.log(c.perf_check.multiplier_max, c.perf_check.multiplier_min)"` → `1.5 0.7`
6. `grep "Format note:" .claude/skills/writing/SKILL.md` → ≥1 match
7. `grep "XML format" .claude/skills/writing/SKILL.md` → ≥1 match

All seven checks must pass.
</verification>

<success_criteria>
1. `/review` SKILL.md schedule table says "Active" for perf-check and contains no "Not yet created" strings; Step 5 pointer references `/generate-content <idea_id>` with no stale `/linkedin` or `/tiktok-slideshows` references
2. `config/schedule-defaults.json` parses as valid JSON and contains `perf_check.multiplier_max = 1.5` and `perf_check.multiplier_min = 0.7`, which `scripts/apply-performance-weights.js` consumes via its existing `loadMultiplierBounds()` function
3. `.claude/skills/writing/SKILL.md` contains an explicit "Format note" documenting that voice profiles use XML (not JSON) and warning against `JSON.parse()` on them
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt-cleanup/06-01-SUMMARY.md` documenting:
- Each of the three edits with before/after snippets
- Confirmation that `scripts/apply-performance-weights.js` behavior is unchanged (values match the prior hardcoded defaults)
- Any grep counts from the verification checks
</output>
